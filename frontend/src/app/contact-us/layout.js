import { pageMeta } from "@/lib/siteMeta";

export const metadata = pageMeta({
    title: "Contact Lankan Ads | Customer Support Sri Lanka | Call Now",
    description:
        "Need help with your ads? Contact Lankan Ads today for reliable customer support. Get in touch with our expert team for any inquiries and assistance.",
    path: "/contact-us",
});

export default function ContactUsLayout({ children }) {
    return children;
}
