import { pageMeta } from "@/lib/siteMeta";

export const metadata = pageMeta({
    title: "Lankan Ads Login | Access Your Account | Post Ads Online",
    description:
        "Welcome to the secure Lankan Ads Login page. Access your account today to manage Sri Lanka ads, connect with buyers, and post new ads online easily!",
    path: "/login",
});

export default function LoginLayout({ children }) {
    return children;
}
